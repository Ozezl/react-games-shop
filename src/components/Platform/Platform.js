import React from 'react';
import './Platform.css';

export default function Platform(props) {
    return (
        <div className = "platform">
            {props.platform}
        </div>
    )    
}